export default function(router){
	console.log(router)
	/*import foo from './foo'
    import bar from './bar'

     router.map({
        '/foo':{
   	         component:foo
        },
        '/bar':{
   	         component:bar
        }
     })
     */
}